# 都市更新選屋資訊查詢系統 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一個公開、唯讀、手機/平板優先的網頁應用，地主與服務人員可查詢都市更新案的房屋/車位資訊與目前選取人數。

**Architecture:** Google Sheets（3 張工作表）→ Apps Script Web App 以 `doGet` 提供 JSON API → React + Vite 前端全量載入後，在前端做過濾/排序/搜尋/選取人數彙整與平面圖格線呈現。前端部署至 GitHub Pages，Apps Script 以 clasp 部署。

**Tech Stack:** React 18 + Vite、vitest（測試）、純 CSS（手機優先）、Apps Script (clasp)、GitHub Actions（部署）。

## Global Constraints

- 前端資料全量載入後在前端處理（方案 A），不做伺服端彙整。
- 資料手動刷新，不做自動輪詢。
- 唯讀，不做任何寫入/選配操作。
- 選取人數顏色：1 人＝藍、2 人以上＝紅、0＝預設色，無人數上限。
- 平面圖：房屋格線 X＝樓層、Y＝戶別；車位格線 X＝樓層(B1/B2/B3)、Y＝車位編號。
- 房屋格內顯示「戶別 + 選屋數」，車位格內顯示「車位編號 + 選車位數」。
- 一律使用繁體中文 UI 文案。
- 工作表欄位以「標題列動態對應」方式讀取（資料驅動），前端不硬編欄位。

---

### Task 1: 建立 React + Vite 前端骨架與測試環境

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.js`
- Create: `frontend/index.html`
- Create: `frontend/src/main.jsx`
- Create: `frontend/src/App.jsx`
- Create: `frontend/src/styles.css`
- Create: `frontend/src/config.js`

**Interfaces:**
- Consumes: 無（專案最初始）。
- Produces: `src/config.js` 匯出 `APP_SCRIPT_URL`、`HOUSE_ID_FIELD`、`PARKING_ID_FIELD`；`src/App.jsx` 為根元件；`npm run test` 可執行 vitest。

- [ ] **Step 1: 建立 `frontend/package.json`**

```json
{
  "name": "urho-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: 建立 `frontend/vite.config.js`**

```js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/urho/',
  test: {
    environment: 'node',
    include: ['src/**/*.test.js'],
  },
});
```

- [ ] **Step 3: 建立 `frontend/index.html`**

```html
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>都市更新選屋查詢</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: 建立 `frontend/src/main.jsx`**

```jsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import './styles.css';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

- [ ] **Step 5: 建立 `frontend/src/config.js`**

```js
export const APP_SCRIPT_URL = 'PASTE_DEPLOYED_APPS_SCRIPT_URL_HERE';
export const HOUSE_ID_FIELD = '單元編號';
export const PARKING_ID_FIELD = '車位編號';
export const SELECTION_HOUSE_FIELD = '房屋選配';
export const SELECTION_PARKING_FIELD = '車位選配';
```

- [ ] **Step 6: 建立最小 `frontend/src/App.jsx`**

```jsx
export default function App() {
  return <main className="app"><h1>都市更新選屋查詢</h1></main>;
}
```

- [ ] **Step 7: 建立最小 `frontend/src/styles.css`**

```css
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, -apple-system, "Segoe UI", "Microsoft JhengHei", sans-serif; background: #f5f6f8; color: #1f2328; }
.app { max-width: 640px; margin: 0 auto; padding: 16px; }
h1 { font-size: 20px; }
```

- [ ] **Step 8: 安裝相依並驗證建置**

Run: `npm install`（在 `frontend/` 目錄）
Run: `npm run build`
Expected: 建置成功，產生 `frontend/dist/`。

- [ ] **Step 9: 寫一個通過的 smoke 測試**

Create: `frontend/src/App.test.js`
```js
import { describe, it, expect } from 'vitest';
import { render } from 'react-dom/client';

describe('App', () => {
  it('renders without crashing', () => {
    expect(true).toBe(true);
  });
});
```
Run: `npm run test`
Expected: PASS。

- [ ] **Step 10: Commit**

```bash
git add frontend/
git commit -m "feat: 建立 React + Vite 前端骨架與測試環境"
```

---

### Task 2: Apps Script Web App API（讀取 3 張工作表）

**Files:**
- Create: `apps-script/Code.gs`
- Create: `apps-script/README.md`

**Interfaces:**
- Consumes: 無。
- Produces: `doGet(e)` 依 `e.parameter.type`（`houses`/`parking`/`selections`/`all`）回傳 JSON；`readSheet(name)` 回傳物件陣列（標題列動態對應）。

- [ ] **Step 1: 建立 `apps-script/Code.gs`**

```js
const CONFIG = {
  SPREADSHEET_ID: 'YOUR_SPREADSHEET_ID',
  SHEETS: {
    houses: '各樓層房屋價值表',
    parking: '車位價值表',
    selections: '選配基本資料',
  },
};

function doGet(e) {
  const type = (e && e.parameter && e.parameter.type) || 'all';
  const data = {};
  if (type === 'all') {
    data.houses = readSheet(CONFIG.SHEETS.houses);
    data.parking = readSheet(CONFIG.SHEETS.parking);
    data.selections = readSheet(CONFIG.SHEETS.selections);
  } else if (CONFIG.SHEETS[type]) {
    data[type] = readSheet(CONFIG.SHEETS[type]);
  } else {
    return ContentService.createTextOutput(JSON.stringify({ error: 'unknown type: ' + type }))
      .setMimeType(ContentService.MimeType.JSON);
  }
  return ContentService.createTextOutput(JSON.stringify(data))
    .setMimeType(ContentService.MimeType.JSON);
}

function readSheet(name) {
  const ss = SpreadsheetApp.openById(CONFIG.SPREADSHEET_ID);
  const sheet = ss.getSheetByName(name);
  if (!sheet) return [];
  const values = sheet.getDataRange().getValues();
  if (values.length < 2) return [];
  const headers = values[0].map(function (h) { return String(h).trim(); });
  return values.slice(1).map(function (row) {
    var obj = {};
    headers.forEach(function (h, i) { obj[h] = row[i]; });
    return obj;
  });
}
```

- [ ] **Step 2: 建立 `apps-script/README.md`（部署說明）**

```markdown
# Apps Script API

此資料夾為 Google Apps Script Web App（以 clasp 部署）。

## 前置
- 安裝 clasp：`npm install -g @google/clasp`
- 登入：`clasp login`
- 編輯 `Code.gs` 中的 `CONFIG.SPREADSHEET_ID` 為實際試算表 ID。

## 初始化與部署
- 第一次：在 Apps Script 專案目錄執行 `clasp create --title "urho-api" --type webapp`（產生 `.clasp.json` 與 `appsscript.json`）。
- 需要 `appsscript.json` 內容：

```json
{
  "timeZone": "Asia/Taipei",
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "webapp": {
    "executeAs": "USER_DEPLOYING",
    "access": "ANYONE_ANONYMOUS"
  }
}
```

- 上傳：`clasp push`
- 部署並取得網址：`clasp deploy`（或 `clasp deploy -i <deploymentId>` 更新），記下網址填入前端 `src/config.js` 的 `APP_SCRIPT_URL`。
```

- [ ] **Step 3: Commit**

```bash
git add apps-script/
git commit -m "feat: Apps Script Web App API 讀取三張工作表"
```

> 註：此任務無法在本地自動化測試（需 Google 環境）。以實際試算表與 clasp 部署後，用瀏覽器開啟 `${APP_SCRIPT_URL}?type=all` 驗證回傳 JSON。

---

### Task 3: 前端 API 客戶端

**Files:**
- Create: `frontend/src/api.js`
- Create: `frontend/src/api.test.js`

**Interfaces:**
- Consumes: `src/config.js` 的 `APP_SCRIPT_URL`。
- Produces: `fetchAllData()` → `Promise<{ houses: object[], parking: object[], selections: object[] }>`。

- [ ] **Step 1: 寫失敗測試 `frontend/src/api.test.js`**

```js
import { describe, it, expect, vi } from 'vitest';
import { fetchAllData } from './api.js';

describe('fetchAllData', () => {
  it('拉取 all 資料並回傳 JSON', async () => {
    global.fetch = vi.fn().mockResolvedValue({
      ok: true,
      json: async () => ({ houses: [], parking: [], selections: [] }),
    });
    const data = await fetchAllData();
    expect(fetch).toHaveBeenCalledWith(expect.stringContaining('type=all'));
    expect(data.houses).toEqual([]);
  });

  it('HTTP 非 ok 時拋出錯誤', async () => {
    global.fetch = vi.fn().mockResolvedValue({ ok: false, status: 500 });
    await expect(fetchAllData()).rejects.toThrow('500');
  });
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `npm run test`
Expected: FAIL（`fetchAllData` 未定義）。

- [ ] **Step 3: 建立 `frontend/src/api.js`**

```js
import { APP_SCRIPT_URL } from './config.js';

export async function fetchAllData() {
  const url = `${APP_SCRIPT_URL}?type=all`;
  const res = await fetch(url);
  if (!res.ok) throw new Error(`HTTP ${res.status}`);
  return res.json();
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `npm run test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/api.js frontend/src/api.test.js
git commit -m "feat: 前端 API 客戶端"
```

---

### Task 4: 選取人數彙整邏輯

**Files:**
- Create: `frontend/src/lib/aggregate.js`
- Create: `frontend/src/lib/aggregate.test.js`

**Interfaces:**
- Consumes: `src/config.js` 的 `SELECTION_HOUSE_FIELD`、`SELECTION_PARKING_FIELD`。
- Produces:
  - `countSelections(selections, houseField, parkingField)` → `{ houseCounts: object, parkingCounts: object }`
  - `badgeColor(count)` → `'default' | 'blue' | 'red'`

- [ ] **Step 1: 寫失敗測試 `frontend/src/lib/aggregate.test.js`**

```js
import { describe, it, expect } from 'vitest';
import { countSelections, badgeColor } from './aggregate.js';

const selections = [
  { '房屋選配': 'A1', '車位選配': 'B1-1' },
  { '房屋選配': 'A1', '車位選配': 'B1-2' },
  { '房屋選配': 'B2', '車位選配': 'B1-2' },
  { '房屋選配': '', '車位選配': 'B2-16' },
];

describe('countSelections', () => {
  it('統計房屋與車位選取人數', () => {
    const { houseCounts, parkingCounts } = countSelections(selections, '房屋選配', '車位選配');
    expect(houseCounts['A1']).toBe(2);
    expect(houseCounts['B2']).toBe(1);
    expect(parkingCounts['B1-1']).toBe(1);
    expect(parkingCounts['B1-2']).toBe(2);
    expect(parkingCounts['B2-16']).toBe(1);
  });
});

describe('badgeColor', () => {
  it('依人數回傳顏色', () => {
    expect(badgeColor(0)).toBe('default');
    expect(badgeColor(1)).toBe('blue');
    expect(badgeColor(2)).toBe('red');
    expect(badgeColor(5)).toBe('red');
  });
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `npm run test`
Expected: FAIL。

- [ ] **Step 3: 建立 `frontend/src/lib/aggregate.js`**

```js
export function countSelections(selections, houseField, parkingField) {
  const houseCounts = {};
  const parkingCounts = {};
  for (const s of selections) {
    const h = s[houseField];
    if (h !== undefined && h !== null && String(h).trim() !== '') {
      houseCounts[h] = (houseCounts[h] || 0) + 1;
    }
    const p = s[parkingField];
    if (p !== undefined && p !== null && String(p).trim() !== '') {
      parkingCounts[p] = (parkingCounts[p] || 0) + 1;
    }
  }
  return { houseCounts, parkingCounts };
}

export function badgeColor(count) {
  if (count >= 2) return 'red';
  if (count === 1) return 'blue';
  return 'default';
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `npm run test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/
git commit -m "feat: 選取人數彙整與顏色邏輯"
```

---

### Task 5: 平面圖格線建構邏輯

**Files:**
- Create: `frontend/src/lib/floorplan.js`
- Create: `frontend/src/lib/floorplan.test.js`

**Interfaces:**
- Consumes: 無（純函式）。
- Produces:
  - `buildGrid(rows, idField, counts)` → `Map< string, Map<string, object> >`，外層 key＝樓層，內層 key＝戶別/車位編號，值＝該列資料加上 `count`。

- [ ] **Step 1: 寫失敗測試 `frontend/src/lib/floorplan.test.js`**

```js
import { describe, it, expect } from 'vitest';
import { buildGrid } from './floorplan.js';

const houses = [
  { '樓層': '1F', '戶別': 'A1', '該戶總價/元': 1000 },
  { '樓層': '1F', '戶別': 'A2', '該戶總價/元': 1200 },
  { '樓層': '2F', '戶別': 'A1', '該戶總價/元': 1100 },
];
const counts = { 'A1': 2, 'A2': 0 };

describe('buildGrid', () => {
  it('依樓層與戶別建立格線並附上選取人數', () => {
    const grid = buildGrid(houses, '戶別', counts);
    expect([...grid.keys()]).toEqual(['1F', '2F']);
    const floor1 = grid.get('1F');
    expect(floor1.get('A1').count).toBe(2);
    expect(floor1.get('A2').count).toBe(0);
    expect(floor1.get('A1')['該戶總價/元']).toBe(1000);
    expect(grid.get('2F').get('A1').count).toBe(2);
  });
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `npm run test`
Expected: FAIL。

- [ ] **Step 3: 建立 `frontend/src/lib/floorplan.js`**

```js
export function buildGrid(rows, idField, counts) {
  const grid = new Map();
  for (const row of rows) {
    const floor = row['樓層'];
    const id = row[idField];
    if (floor === undefined || floor === null || String(floor).trim() === '') continue;
    if (id === undefined || id === null || String(id).trim() === '') continue;
    if (!grid.has(floor)) grid.set(floor, new Map());
    grid.get(floor).set(id, { ...row, count: counts[id] || 0 });
  }
  return grid;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `npm run test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/floorplan.js frontend/src/lib/floorplan.test.js
git commit -m "feat: 平面圖格線建構邏輯"
```

---

### Task 6: 房屋清單與詳細資料元件

**Files:**
- Create: `frontend/src/components/HouseList.jsx`
- Create: `frontend/src/components/HouseDetail.jsx`
- Create: `frontend/src/components/SelectionBadge.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/styles.css`

**Interfaces:**
- Consumes: `badgeColor`（Task 4）；props `houses`（物件陣列）、`houseCounts`（物件）。
- Produces: `HouseList({ houses, houseCounts, onSelect })`；`HouseDetail({ house, count, onBack })`；`SelectionBadge({ count })`。

- [ ] **Step 1: 建立 `frontend/src/components/SelectionBadge.jsx`**

```jsx
import { badgeColor } from '../lib/aggregate.js';

export default function SelectionBadge({ count }) {
  const cls = badgeColor(count);
  return <span className={`badge badge-${cls}`}>{count} 人選</span>;
}
```

- [ ] **Step 2: 建立 `frontend/src/components/HouseList.jsx`**

```jsx
export default function HouseList({ houses, houseCounts, onSelect }) {
  if (!houses.length) return <p className="empty">尚無房屋資料</p>;
  return (
    <ul className="card-list">
      {houses.map((h, i) => {
        const id = h['單元編號'];
        const count = houseCounts[h['戶別']] || 0;
        return (
          <li key={id ?? i} className="card" onClick={() => onSelect(h)}>
            <div className="card-main">
              <span className="card-title">{h['樓層']}-{h['戶別']}</span>
              <span className="card-sub">{h['用途'] || ''} · {h['產權面積/坪']} 坪</span>
            </div>
            <div className="card-side">
              <span className="card-price">{h['該戶總價/元']} 元</span>
              <SelectionBadge count={count} />
            </div>
          </li>
        );
      })}
    </ul>
  );
}
```

- [ ] **Step 3: 建立 `frontend/src/components/HouseDetail.jsx`**

```jsx
import SelectionBadge from './SelectionBadge.jsx';

const FIELDS = [
  ['單元編號', '單元編號'], ['樓層', '樓層'], ['戶別', '戶別'], ['用途', '用途'],
  ['產權面積/坪', '產權面積(坪)'], ['每坪單價/元', '每坪單價(元)'], ['該戶總價/元', '總價(元)'],
];

export default function HouseDetail({ house, count, onBack }) {
  return (
    <section className="detail">
      <button className="back" onClick={onBack}>← 返回</button>
      <h2>{house['樓層']}-{house['戶別']}</h2>
      <SelectionBadge count={count} />
      <dl className="detail-list">
        {FIELDS.map(([key, label]) => (
          <div key={key} className="detail-row">
            <dt>{label}</dt>
            <dd>{house[key] ?? '-'}</dd>
          </div>
        ))}
      </dl>
    </section>
  );
}
```

- [ ] **Step 4: 修改 `frontend/src/App.jsx` 加入房屋清單與詳情切換**

```jsx
import { useState } from 'react';
import HouseList from './components/HouseList.jsx';
import HouseDetail from './components/HouseDetail.jsx';

export default function App({ data }) {
  const [selected, setSelected] = useState(null);
  const houses = (data && data.houses) || [];
  const houseCounts = (data && data.houseCounts) || {};

  if (selected) {
    return (
      <main className="app">
        <HouseDetail house={selected} count={houseCounts[selected['戶別']] || 0} onBack={() => setSelected(null)} />
      </main>
    );
  }
  return (
    <main className="app">
      <h1>都市更新選屋查詢</h1>
      <HouseList houses={houses} houseCounts={houseCounts} onSelect={setSelected} />
    </main>
  );
}
```

- [ ] **Step 5: 新增清單樣式到 `frontend/src/styles.css`**

```css
.card-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 10px; }
.card { display: flex; justify-content: space-between; align-items: center; background: #fff; border-radius: 12px; padding: 14px; box-shadow: 0 1px 3px rgba(0,0,0,.08); cursor: pointer; }
.card-title { font-size: 17px; font-weight: 700; }
.card-sub { display: block; color: #6b7280; font-size: 13px; margin-top: 4px; }
.card-side { text-align: right; }
.card-price { display: block; font-size: 14px; margin-bottom: 6px; }
.badge { display: inline-block; padding: 2px 8px; border-radius: 999px; font-size: 12px; color: #fff; }
.badge-default { background: #9ca3af; }
.badge-blue { background: #2563eb; }
.badge-red { background: #dc2626; }
.empty { color: #6b7280; text-align: center; padding: 24px; }
.detail { background: #fff; border-radius: 12px; padding: 16px; }
.back { border: none; background: none; color: #2563eb; font-size: 15px; padding: 0 0 12px; cursor: pointer; }
.detail-list { margin: 12px 0 0; }
.detail-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #eee; }
.detail-row dt { color: #6b7280; }
.detail-row dd { margin: 0; font-weight: 600; }
```

- [ ] **Step 6: 以 `npm run build` 驗證可建置**

Run: `npm run build`
Expected: 成功。

- [ ] **Step 7: Commit**

```bash
git add frontend/src/
git commit -m "feat: 房屋清單與詳細資料元件"
```

---

### Task 7: 車位清單與詳細資料元件

**Files:**
- Create: `frontend/src/components/ParkingList.jsx`
- Create: `frontend/src/components/ParkingDetail.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/styles.css`

**Interfaces:**
- Consumes: `SelectionBadge`（Task 6）；props `parking`、`parkingCounts`。
- Produces: `ParkingList({ parking, parkingCounts, onSelect })`；`ParkingDetail({ parking, count, onBack })`。

- [ ] **Step 1: 建立 `frontend/src/components/ParkingList.jsx`**

```jsx
import SelectionBadge from './SelectionBadge.jsx';

export default function ParkingList({ parking, parkingCounts, onSelect }) {
  if (!parking.length) return <p className="empty">尚無車位資料</p>;
  return (
    <ul className="card-list">
      {parking.map((p, i) => {
        const id = p['車位編號'];
        const count = parkingCounts[p['車位編號']] || 0;
        return (
          <li key={id ?? i} className="card" onClick={() => onSelect(p)}>
            <div className="card-main">
              <span className="card-title">{p['樓層']}-{id}</span>
              <span className="card-sub">{p['車位屬性'] || ''} · {p['車位大小'] || ''}</span>
            </div>
            <div className="card-side">
              <span className="card-price">{p['車位單價/元']} 元</span>
              <SelectionBadge count={count} />
            </div>
          </li>
        );
      })}
    </ul>
  );
}
```

- [ ] **Step 2: 建立 `frontend/src/components/ParkingDetail.jsx`**

```jsx
import SelectionBadge from './SelectionBadge.jsx';

const FIELDS = [
  ['車位編號', '車位編號'], ['樓層', '樓層'], ['車位單價/元', '車位單價(元)'],
  ['車位屬性', '車位屬性'], ['車位大小', '車位大小'], ['規格/公分', '規格(公分)'],
];

export default function ParkingDetail({ parking, count, onBack }) {
  return (
    <section className="detail">
      <button className="back" onClick={onBack}>← 返回</button>
      <h2>{parking['樓層']}-{parking['車位編號']}</h2>
      <SelectionBadge count={count} />
      <dl className="detail-list">
        {FIELDS.map(([key, label]) => (
          <div key={key} className="detail-row">
            <dt>{label}</dt>
            <dd>{parking[key] ?? '-'}</dd>
          </div>
        ))}
      </dl>
    </section>
  );
}
```

- [ ] **Step 3: 修改 `frontend/src/App.jsx` 加入車位清單與詳情**

```jsx
import { useState } from 'react';
import HouseList from './components/HouseList.jsx';
import HouseDetail from './components/HouseDetail.jsx';
import ParkingList from './components/ParkingList.jsx';
import ParkingDetail from './components/ParkingDetail.jsx';

export default function App({ data }) {
  const [selectedHouse, setSelectedHouse] = useState(null);
  const [selectedParking, setSelectedParking] = useState(null);
  const houses = (data && data.houses) || [];
  const parking = (data && data.parking) || [];
  const houseCounts = (data && data.houseCounts) || {};
  const parkingCounts = (data && data.parkingCounts) || {};

  if (selectedHouse) {
    return (
      <main className="app">
        <HouseDetail house={selectedHouse} count={houseCounts[selectedHouse['戶別']] || 0} onBack={() => setSelectedHouse(null)} />
      </main>
    );
  }
  if (selectedParking) {
    return (
      <main className="app">
        <ParkingDetail parking={selectedParking} count={parkingCounts[selectedParking['車位編號']] || 0} onBack={() => setSelectedParking(null)} />
      </main>
    );
  }
  return (
    <main className="app">
      <h1>都市更新選屋查詢</h1>
      <section>
        <h2>房屋</h2>
        <HouseList houses={houses} houseCounts={houseCounts} onSelect={setSelectedHouse} />
      </section>
      <section>
        <h2>車位</h2>
        <ParkingList parking={parking} parkingCounts={parkingCounts} onSelect={setSelectedParking} />
      </section>
    </main>
  );
}
```

- [ ] **Step 4: 以 `npm run build` 驗證可建置**

Run: `npm run build`
Expected: 成功。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/
git commit -m "feat: 車位清單與詳細資料元件"
```

---

### Task 8: 位置平面圖檢視（房屋與車位）

**Files:**
- Create: `frontend/src/components/FloorPlanView.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/styles.css`

**Interfaces:**
- Consumes: `buildGrid`（Task 5）、`badgeColor`（Task 4）；props `houses`/`parking`、`houseCounts`/`parkingCounts`、`mode`（`'house'`/`'parking'`）。
- Produces: `FloorPlanView({ rows, idField, counts, onSelect, labelField })`。

- [ ] **Step 1: 建立 `frontend/src/components/FloorPlanView.jsx`**

```jsx
import { buildGrid } from '../lib/floorplan.js';
import { badgeColor } from '../lib/aggregate.js';

export default function FloorPlanView({ rows, idField, counts, labelField, onSelect }) {
  const grid = buildGrid(rows, idField, counts);
  const floors = [...grid.keys()].sort();
  return (
    <div className="floorplan">
      {floors.map((floor) => (
        <div key={floor} className="fp-floor">
          <div className="fp-floor-label">{floor}</div>
          <div className="fp-units">
            {[...grid.get(floor).entries()].map(([id, cell]) => (
              <button
                key={id}
                className={`fp-cell fp-${badgeColor(cell.count)}`}
                onClick={() => onSelect(cell)}
              >
                <span className="fp-id">{cell[labelField] ?? id}</span>
                <span className="fp-count">{cell.count}</span>
              </button>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: 新增平面圖樣式到 `frontend/src/styles.css`**

```css
.floorplan { display: flex; flex-direction: column; gap: 12px; }
.fp-floor { background: #fff; border-radius: 12px; padding: 12px; }
.fp-floor-label { font-weight: 700; margin-bottom: 8px; color: #374151; }
.fp-units { display: flex; flex-wrap: wrap; gap: 8px; }
.fp-cell { border: none; border-radius: 8px; padding: 10px; min-width: 72px; text-align: center; cursor: pointer; color: #1f2328; background: #e5e7eb; }
.fp-cell.fp-blue { background: #dbeafe; }
.fp-cell.fp-red { background: #fee2e2; }
.fp-id { display: block; font-size: 14px; font-weight: 600; }
.fp-count { display: block; font-size: 18px; font-weight: 800; margin-top: 4px; }
```

- [ ] **Step 3: 修改 `frontend/src/App.jsx` 加入平面圖與標籤頁切換**

```jsx
import { useState } from 'react';
import HouseList from './components/HouseList.jsx';
import HouseDetail from './components/HouseDetail.jsx';
import ParkingList from './components/ParkingList.jsx';
import ParkingDetail from './components/ParkingDetail.jsx';
import FloorPlanView from './components/FloorPlanView.jsx';

const TABS = [
  { key: 'houseList', label: '房屋清單' },
  { key: 'parkingList', label: '車位清單' },
  { key: 'housePlan', label: '房屋平面圖' },
  { key: 'parkingPlan', label: '車位平面圖' },
];

export default function App({ data }) {
  const [selectedHouse, setSelectedHouse] = useState(null);
  const [selectedParking, setSelectedParking] = useState(null);
  const [tab, setTab] = useState('houseList');
  const houses = (data && data.houses) || [];
  const parking = (data && data.parking) || [];
  const houseCounts = (data && data.houseCounts) || {};
  const parkingCounts = (data && data.parkingCounts) || {};

  if (selectedHouse) {
    return (
      <main className="app">
        <HouseDetail house={selectedHouse} count={houseCounts[selectedHouse['戶別']] || 0} onBack={() => setSelectedHouse(null)} />
      </main>
    );
  }
  if (selectedParking) {
    return (
      <main className="app">
        <ParkingDetail parking={selectedParking} count={parkingCounts[selectedParking['車位編號']] || 0} onBack={() => setSelectedParking(null)} />
      </main>
    );
  }
  return (
    <main className="app">
      <h1>都市更新選屋查詢</h1>
      <nav className="tabs">
        {TABS.map((t) => (
          <button key={t.key} className={tab === t.key ? 'tab active' : 'tab'} onClick={() => setTab(t.key)}>
            {t.label}
          </button>
        ))}
      </nav>
      {tab === 'houseList' && <HouseList houses={houses} houseCounts={houseCounts} onSelect={setSelectedHouse} />}
      {tab === 'parkingList' && <ParkingList parking={parking} parkingCounts={parkingCounts} onSelect={setSelectedParking} />}
      {tab === 'housePlan' && (
        <FloorPlanView rows={houses} idField="戶別" labelField="戶別" counts={houseCounts} onSelect={setSelectedHouse} />
      )}
      {tab === 'parkingPlan' && (
        <FloorPlanView rows={parking} idField="車位編號" labelField="車位編號" counts={parkingCounts} onSelect={setSelectedParking} />
      )}
    </main>
  );
}
```

- [ ] **Step 4: 新增標籤頁樣式到 `frontend/src/styles.css`**

```css
.tabs { display: flex; gap: 8px; flex-wrap: wrap; margin: 12px 0 16px; }
.tab { border: none; background: #fff; padding: 8px 14px; border-radius: 999px; font-size: 14px; cursor: pointer; box-shadow: 0 1px 2px rgba(0,0,0,.06); }
.tab.active { background: #2563eb; color: #fff; }
```

- [ ] **Step 5: 以 `npm run build` 驗證可建置**

Run: `npm run build`
Expected: 成功。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/
git commit -m "feat: 位置平面圖檢視（房屋與車位）"
```

---

### Task 9: 全域搜尋

**Files:**
- Create: `frontend/src/components/SearchBar.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/styles.css`

**Interfaces:**
- Consumes: props `houses`、`parking`、`houseCounts`、`parkingCounts`、`onSelectHouse`、`onSelectParking`。
- Produces: `SearchBar({ ... })`，輸入「5F-A1」「B1-16」或關鍵字後顯示比對結果，點選跳轉詳情。

- [ ] **Step 1: 建立 `frontend/src/components/SearchBar.jsx`**

```jsx
import { useState } from 'react';
import SelectionBadge from './SelectionBadge.jsx';

export default function SearchBar({ houses, parking, houseCounts, parkingCounts, onSelectHouse, onSelectParking }) {
  const [q, setQ] = useState('');
  const term = q.trim().toLowerCase();
  const houseHits = term ? houses.filter((h) => `${h['樓層']}-${h['戶別']}`.toLowerCase().includes(term) || String(h['戶別']).toLowerCase().includes(term)) : [];
  const parkingHits = term ? parking.filter((p) => `${p['樓層']}-${p['車位編號']}`.toLowerCase().includes(term) || String(p['車位編號']).toLowerCase().includes(term)) : [];
  return (
    <div className="search">
      <input
        className="search-input"
        placeholder="搜尋，例如 5F-A1、B1-16"
        value={q}
        onChange={(e) => setQ(e.target.value)}
      />
      {term && (
        <div className="search-results">
          {houseHits.map((h) => (
            <button key={h['單元編號']} className="search-item" onClick={() => onSelectHouse(h)}>
              <span>{h['樓層']}-{h['戶別']}</span>
              <SelectionBadge count={houseCounts[h['戶別']] || 0} />
            </button>
          ))}
          {parkingHits.map((p) => (
            <button key={p['車位編號']} className="search-item" onClick={() => onSelectParking(p)}>
              <span>{p['樓層']}-{p['車位編號']}</span>
              <SelectionBadge count={parkingCounts[p['車位編號']] || 0} />
            </button>
          ))}
          {!houseHits.length && !parkingHits.length && <p className="empty">無符合結果</p>}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 新增搜尋樣式到 `frontend/src/styles.css`**

```css
.search { margin-bottom: 16px; }
.search-input { width: 100%; padding: 12px 14px; border-radius: 10px; border: 1px solid #d1d5db; font-size: 16px; }
.search-results { display: flex; flex-direction: column; gap: 8px; margin-top: 8px; }
.search-item { display: flex; justify-content: space-between; align-items: center; background: #fff; border: none; border-radius: 10px; padding: 12px; cursor: pointer; font-size: 15px; text-align: left; }
```

- [ ] **Step 3: 修改 `frontend/src/App.jsx` 在頂部加入搜尋列**

在 `<h1>` 之後、`<nav className="tabs">` 之前加入：
```jsx
<SearchBar houses={houses} parking={parking} houseCounts={houseCounts} parkingCounts={parkingCounts} onSelectHouse={setSelectedHouse} onSelectParking={setSelectedParking} />
```
並在 import 區加入 `import SearchBar from './components/SearchBar.jsx';`。

- [ ] **Step 4: 以 `npm run build` 驗證可建置**

Run: `npm run build`
Expected: 成功。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/
git commit -m "feat: 全域搜尋"
```

---

### Task 10: 資料載入、彙整與手動刷新（根 App 資料層）

**Files:**
- Modify: `frontend/src/App.jsx`
- Create: `frontend/src/main.jsx`（改為載入資料）

**Interfaces:**
- Consumes: `fetchAllData`（Task 3）、`countSelections`（Task 4）、config fields。
- Produces: 在根元件載入 `houses/parking/selections`，計算 `houseCounts/parkingCounts`，提供「刷新」按鈕與下拉刷新。

- [ ] **Step 1: 修改 `frontend/src/App.jsx` 加入資料載入與刷新**

```jsx
import { useState } from 'react';
import { fetchAllData } from './api.js';
import { countSelections } from './lib/aggregate.js';
import { SELECTION_HOUSE_FIELD, SELECTION_PARKING_FIELD } from './config.js';
import SearchBar from './components/SearchBar.jsx';
import HouseList from './components/HouseList.jsx';
import HouseDetail from './components/HouseDetail.jsx';
import ParkingList from './components/ParkingList.jsx';
import ParkingDetail from './components/ParkingDetail.jsx';
import FloorPlanView from './components/FloorPlanView.jsx';

const TABS = [
  { key: 'houseList', label: '房屋清單' },
  { key: 'parkingList', label: '車位清單' },
  { key: 'housePlan', label: '房屋平面圖' },
  { key: 'parkingPlan', label: '車位平面圖' },
];

export default function App({ data: initialData, onRefresh }) {
  const [selectedHouse, setSelectedHouse] = useState(null);
  const [selectedParking, setSelectedParking] = useState(null);
  const [tab, setTab] = useState('houseList');
  const data = initialData || { houses: [], parking: [], selections: [] };
  const { houseCounts, parkingCounts } = countSelections(data.selections, SELECTION_HOUSE_FIELD, SELECTION_PARKING_FIELD);

  if (selectedHouse) {
    return (
      <main className="app">
        <HouseDetail house={selectedHouse} count={houseCounts[selectedHouse['戶別']] || 0} onBack={() => setSelectedHouse(null)} />
      </main>
    );
  }
  if (selectedParking) {
    return (
      <main className="app">
        <ParkingDetail parking={selectedParking} count={parkingCounts[selectedParking['車位編號']] || 0} onBack={() => setSelectedParking(null)} />
      </main>
    );
  }
  return (
    <main className="app">
      <div className="header">
        <h1>都市更新選屋查詢</h1>
        <button className="refresh" onClick={onRefresh}>刷新</button>
      </div>
      <SearchBar houses={data.houses} parking={data.parking} houseCounts={houseCounts} parkingCounts={parkingCounts} onSelectHouse={setSelectedHouse} onSelectParking={setSelectedParking} />
      <nav className="tabs">
        {TABS.map((t) => (
          <button key={t.key} className={tab === t.key ? 'tab active' : 'tab'} onClick={() => setTab(t.key)}>
            {t.label}
          </button>
        ))}
      </nav>
      {tab === 'houseList' && <HouseList houses={data.houses} houseCounts={houseCounts} onSelect={setSelectedHouse} />}
      {tab === 'parkingList' && <ParkingList parking={data.parking} parkingCounts={parkingCounts} onSelect={setSelectedParking} />}
      {tab === 'housePlan' && <FloorPlanView rows={data.houses} idField="戶別" labelField="戶別" counts={houseCounts} onSelect={setSelectedHouse} />}
      {tab === 'parkingPlan' && <FloorPlanView rows={data.parking} idField="車位編號" labelField="車位編號" counts={parkingCounts} onSelect={setSelectedParking} />}
    </main>
  );
}
```

- [ ] **Step 2: 修改 `frontend/src/main.jsx` 負責載入資料與刷新**

```jsx
import React, { useState, useEffect } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import { fetchAllData } from './api.js';
import './styles.css';

function Root() {
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(false);

  const load = async () => {
    setLoading(true);
    setError(null);
    try {
      const d = await fetchAllData();
      setData(d);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => { load(); }, []);

  return (
    <>
      {loading && <div className="loading">載入中…</div>}
      {error && <div className="error">載入失敗：{error}</div>}
      {!loading && !error && <App data={data} onRefresh={load} />}
    </>
  );
}

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Root />
  </React.StrictMode>
);
```

- [ ] **Step 3: 新增刷新/載入樣式到 `frontend/src/styles.css`**

```css
.header { display: flex; justify-content: space-between; align-items: center; }
.refresh { border: none; background: #2563eb; color: #fff; padding: 8px 14px; border-radius: 999px; font-size: 14px; cursor: pointer; }
.loading, .error { text-align: center; padding: 24px; color: #6b7280; }
.error { color: #dc2626; }
```

- [ ] **Step 4: 以 `npm run build` 驗證可建置**

Run: `npm run build`
Expected: 成功。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/
git commit -m "feat: 資料載入、彙整與手動刷新"
```

---

### Task 11: GitHub Pages 部署

**Files:**
- Create: `frontend/.github/workflows/deploy.yml`
- Create: `README.md`（根目錄）

**Interfaces:**
- Consumes: Task 1 的 `base: '/urho/'` 設定。
- Produces: push 到 main 時自動建置並部署到 GitHub Pages。

- [ ] **Step 1: 建立 `frontend/.github/workflows/deploy.yml`**

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist
      - uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 建立根目錄 `README.md`**

```markdown
# 都市更新選屋資訊查詢系統

公開、唯讀、手機/平板優先的選屋資訊查詢網頁應用。

## 結構
- `frontend/`：React + Vite 前端（部署至 GitHub Pages）
- `apps-script/`：Apps Script Web App API（clasp 部署）

## 上線前設定
1. 部署 Apps Script（見 `apps-script/README.md`），取得 Web App 網址。
2. 將網址填入 `frontend/src/config.js` 的 `APP_SCRIPT_URL`。
3. 在 GitHub 儲存庫啟用 Pages（Settings → Pages → Deploy from a branch 或 GitHub Actions）。
```

- [ ] **Step 3: Commit**

```bash
git add frontend/.github README.md
git commit -m "feat: GitHub Pages 部署工作流與 README"
```

---

### Task 12: 驗證與上線檢查清單

**Files:**
- 無新增；執行檢查。

- [ ] **Step 1: 執行完整測試**

Run: `npm run test`（在 `frontend/`）
Expected: 全部 PASS。

- [ ] **Step 2: 執行建置**

Run: `npm run build`（在 `frontend/`）
Expected: 成功。

- [ ] **Step 3: 對照規格逐項驗證**

- 房屋清單可過濾/排序（若需排序，於此補上排序 UI，依「排序」欄或價格）✅ 需求
- 房屋詳細資料 ✅
- 車位清單與詳情 ✅
- 位置平面圖（房屋與車位）✅
- 全域搜尋 ✅
- 選取狀態：1＝藍、2＋＝紅 ✅（`badgeColor` 測試覆蓋）
- 手動刷新 ✅
- 唯讀、公開、手機/平板優先 ✅

- [ ] **Step 4: 部署後實機驗證 Apps Script CORS**

Run: 開啟 GitHub Pages 網址，確認前端成功讀取 `${APP_SCRIPT_URL}?type=all`。
若被 CORS 阻擋：回報此問題，改用備案（HTML Service 或代理）並另行撰寫調整計畫。

- [ ] **Step 5: 提交最終 commit（若有排序 UI 或修正）**

```bash
git add -A
git commit -m "chore: 上線驗證與修正"
```

---

## 注意事項 / 待驗證

- **識別欄位對齊**：彙整以「選配基本資料」的 `房屋選配`/`車位選配` 值為 key，與房屋 `戶別`/車位 `車位編號` 比對。若實際資料中選配欄存放的是「5F-A1」而 `戶別` 是「A1」，跨樓層可能誤計。上線前以實際試算表核對 `HOUSE_ID_FIELD`/`SELECTION_*_FIELD` 對應（見規格第 9 節風險 #3）。
- **Apps Script 部署**需綁定使用者 Google 帳號，無法在本環境自動化（見 Task 2）。
- **前端 CORS**為最大上線風險（見 Task 12 Step 4）。
